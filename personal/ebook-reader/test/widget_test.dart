import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder smoke test', (WidgetTester tester) async {
    // This test is a placeholder — integration tests in integration_test/
    expect(true, isTrue);
  });
}
