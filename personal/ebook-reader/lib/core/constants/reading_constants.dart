class ReadingConstants {
  // Font size range
  static const double minFontSize = 10.0;
  static const double maxFontSize = 36.0;
  static const double defaultFontSize = 18.0;

  // Line spacing range
  static const double minLineSpacing = 1.0;
  static const double maxLineSpacing = 3.0;
  static const double defaultLineSpacing = 1.5;

  // Paragraph spacing
  static const double defaultParagraphSpacing = 8.0;

  // Margins
  static const double defaultHorizontalMargin = 16.0;
  static const double defaultVerticalMargin = 16.0;

  // Brightness
  static const double defaultBrightness = 0.7;

  // Blue light filter (0 = off, 1 = max warm)
  static const double defaultBlueLightFilter = 0.0;

  // Auto-scroll speed (pixels per second)
  static const double defaultAutoScrollSpeed = 60.0;

  // Page animation duration
  static const int pageAnimationDurationMs = 300;

  // TTS speed range
  static const double minTtsSpeed = 0.5;
  static const double maxTtsSpeed = 3.0;
  static const double defaultTtsSpeed = 1.0;
}
